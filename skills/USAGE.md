# Skill 使用记录（可截断，勿删文件）

IRON：加载即记一行。截断只用 `python3 scripts/compact-usage.py`（Windows 无 python3 则用 `python`；每 id 保留最后一行）。

| 日期 | id（改造层名或 REGISTRY id） | 任务一句话 |
|------|------------------------------|------------|
|  |  |  |
